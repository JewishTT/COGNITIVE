{
  "name": "flowsint-enrichers",
  "version": "0.1.0",
  "description": "Flowsint enrichers — TheBigBrother deep-profiling enricher for the OSINT orchestration layer.",
  "requires-python": ">=3.10",
  "license": "MIT",
  "dependencies": ["requests>=2.28"],
  "optional-dependencies": {
    "dev": ["pytest>=7.0"]
  },
  "tool": {
    "setuptools": {
      "packages": { "find": { "where": ["src"] } }
    }
  }
}
